import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 180,
  },

  title: {
    fontSize: 21,
    textAlign: 'center',
    marginTop: 16,
    fontWeight: 'bold',
    color: '#E4E4E4',
  }
});